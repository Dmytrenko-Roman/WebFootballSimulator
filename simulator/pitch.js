/*'use strict';

const pitchh = {
  xp: 10,
  yp: 10,
  wp: 600,
  hp: 400,
};

const bg = {
  linelength: 40,
  draw: () => {
    ctx.beginPath();
    ctx.fillStyle = 'green';
    ctx.fillRect(0, 0, canvas.width, canvas.height);
    ctx.fillStyle = 'green';
    ctx.fillRect(pitchh.xp, pitchh.yp, pitchh.wp, pitchh.hp);
    ctx.fillStyle = 'darkgreen';
    for (let i = 0; i < wp; i += 80) {
      ctx.fillRect(xp + i, yp, this.linelength, hp);
    }
  }
};

//module.exports = { bg }; */
