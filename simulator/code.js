const canvas = document.getElementById('canvas');
const ctx = canvas.getContext('2d');

const pitchWidth = 540;
const pitchHeigth = 360;

ctx.fillStyle = 'green';
ctx.fillRect(0, 0, pitchWidth, pitchHeigth);